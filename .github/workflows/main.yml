# .github/workflows/main.yml

name: Build und Bereitstellung # Klarer, beschreibender Name für deinen Workflow

on:
  push:
    branches:
      - main # Löst den Workflow bei Pushs auf den 'main'-Branch aus
  pull_request:
    branches:
      - main # Löst den Workflow bei Pull Requests aus, die auf den 'main'-Branch abzielen

jobs:
  build: # Definiere einen Job namens 'build'
    runs-on: ubuntu-latest # Gib die Runner-Umgebung an (Ubuntu ist üblich für Builds)

    # Umgebungsvariablen, die allen Schritten in diesem Job zur Verfügung stehen.
    # Der GITHUB_TOKEN ist für viele GitHub Actions-Funktionen unerlässlich.
    env:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    steps:
      - name: Code auschecken # Schritt zum Abrufen des Repository-Codes
        uses: actions/checkout@v4 # Verwendet die offizielle GitHub Action zum Auschecken von Code

      # Dieser Schritt simuliert das Setzen von Umgebungsvariablen wie TOGA_VERSION und ANDROID_API_LEVEL.
      # Ersetze dies durch deine tatsächliche Logik, um diese Werte zu bestimmen oder zu setzen.
      - name: Build-Umgebungsvariablen setzen
        id: set-build-vars
        run: |
          echo "TOGA_VERSION=0.13.0" >> "$GITHUB_ENV" # Beispiel Toga-Version
          echo "ANDROID_API_LEVEL=33" >> "$GITHUB_ENV" # Beispiel Android API Level

      # Platzhalter-Schritt: Ersetze dies durch deinen tatsächlichen Build-Befehl (z.B. Python, Android Gradle Build).
      # Dieser Schritt sollte auch die 'version' und 'apk-name' ausgeben, die der Zusammenfassungs-Schritt verwenden wird.
      - name: Anwendungssimulation bauen
        id: build-app
        run: |
          echo "--- Hier laufen deine tatsächlichen Build-Befehle ---"
          # Beispiel für das Setzen von Ausgaben, die von nachfolgenden Schritten verwendet werden
          echo "version=1.0.0" >> "$GITHUB_OUTPUT"
          echo "apk-name=DeineApp-Release-1.0.0.apk" >> "$GITHUB_OUTPUT"

      # Platzhalter-Schritt: Ersetze dies durch deine tatsächliche Logik, um die gebaute APK zu finden und ihre Größe zu ermitteln.
      - name: APK finden und Größe ermitteln
        id: locate-apk
        run: |
          echo "--- APK wird gesucht und Größe berechnet ---"
          # Beispiel für das Setzen einer Ausgabe für die APK-Größe
          echo "apk-size-mb=12.5" >> "$GITHUB_OUTPUT"

      - name: Build-Zusammenfassung # Dein ursprünglicher Schritt zur Zusammenfassung des Builds
        # Dieser Schritt verwendet 'run', um Shell-Befehle auszuführen.
        # Er fügt die Build-Informationen der GitHub Actions Schrittzusammenfassung hinzu.
        run: |
          echo "## 🎉 Build erfolgreich abgeschlossen!" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "**Toga:** ${{ env.TOGA_VERSION }}" >> "$GITHUB_STEP_SUMMARY" # Verwende env für zuvor gesetzte Variablen
          echo "**Android API Level:** ${{ env.ANDROID_API_LEVEL }}" >> "$GITHUB_STEP_SUMMARY"
          echo "**Version:** ${{ steps.build-app.outputs.version }}" >> "$GITHUB_STEP_SUMMARY" # Ausgabe vom Schritt 'build-app'
          echo "**APK-Name:** ${{ steps.build-app.outputs.apk-name }}" >> "$GITHUB_STEP_SUMMARY" # Ausgabe vom Schritt 'build-app'
          echo "**APK-Größe:** ${{ steps.locate-apk.outputs.apk-size-mb }}MB" >> "$GITHUB_STEP_SUMMARY" # Ausgabe vom Schritt 'locate-apk'
          echo "**Build-Nummer:** #${{ github.run_number }}" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          
